/**
 * add - a function that adds two munbers
 * @num: the number to be added
 * @digi: the number to be added
 *
 * Return: add
 */

int add(int num, int digi)
{
	int add;

	add = num + digi;
	return (add);
}
