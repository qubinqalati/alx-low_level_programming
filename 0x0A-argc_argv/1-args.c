#include <stdio.h>

/**
 * main - entry point
 * @argc: number of arguments
 *
 * Return: Always success
 */

int main(int argc, char *argv[])
{
	(void)argv;

	printf("%d\n", argc - 1);
	return (0);
}
